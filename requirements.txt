# --- Base (P0) ---
python-dotenv==1.0.1

# --- Tests (obligatoires avant toute exécution) ---
pytest==8.3.3
pytest-cov==5.0.0

# --- Ajoutés au palier P1 (ingestion / extraction) ---
telethon==1.36.0
# oandapyv20 retiré : broker Capital.com, pas OANDA (pivot documenté dans CLAUDE.md)
# requests non ajouté au palier P1 : audit_notifier.py utilise urllib (stdlib)

# --- Ajoutés au palier P2 (exécution démo, analyse de trade) ---
requests==2.34.2       # capital_client.py — déjà une dépendance de fait des scripts P0, jamais déclarée avant
anthropic==0.122.0      # trade_analyzer.py — narratif post-trade uniquement (§3.10), jamais de décision
